# ============================================================
# CONFIGURAÇÕES DO SERVIDOR
# ============================================================
PORT=3000
NODE_ENV=development

# ============================================================
# CONFIGURAÇÕES DO BANCO DE DADOS MYSQL
# ============================================================
# Importante: NÃO usar usuário root em produção
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=app_eventos
MYSQL_PASSWORD=App@2024!
MYSQL_DATABASE=eventosdb
MYSQL_CONNECTION_LIMIT=10

# ============================================================
# CONFIGURAÇÕES DO MONGODB (NoSQL)
# ============================================================
# Para MongoDB local
MONGO_URL=mongodb://localhost:27017/eventosdb

# Para MongoDB Atlas (cloud)
# MONGO_URL=mongodb+srv://usuario:senha@cluster.mongodb.net/eventosdb?retryWrites=true&w=majority

# ============================================================
# CONFIGURAÇÕES DE AUTENTICAÇÃO JWT
# ============================================================
# IMPORTANTE: Em produção, use uma chave forte e aleatória
# Gere com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=chave-secreta-super-segura-mude-em-producao-2024

# ============================================================
# OBSERVAÇÕES
# ============================================================
# 1. Copie este arquivo para .env antes de executar
# 2. NUNCA commite o arquivo .env no git (já está no .gitignore)
# 3. Configure as credenciais do MySQL conforme o script schema.sql
# 4. Para produção, use variáveis de ambiente do servidor/container